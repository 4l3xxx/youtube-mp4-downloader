{
  "name": "youtube-mp4-downloader",
  "version": "1.0.0",
  "private": true,
  "description": "Simple web app to download YouTube videos as MP4 via yt-dlp + ffmpeg",
  "main": "src/server.js",
  "scripts": {
    "prestart": "pip install -U yt-dlp",
    "start": "node src/server.js",
    "dev": "node src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
